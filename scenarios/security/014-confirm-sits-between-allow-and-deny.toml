name = "a confirm rule asks rather than refusing"
tags = ["guard"]

[setup]
confirm = ["^sudo\\b", "git push --force"]

[[step]]
check = "sudo apt install ripgrep"
[[step]]
expect_verdict = "confirm"

[[step]]
check = "apt install ripgrep"
[[step]]
expect_verdict = "allow"
