name = "an ordinary command is allowed with no rules in the way"
tags = ["guard"]

# The default posture is zero friction: nothing denied, nothing required.

[[step]]
check = "git status"
[[step]]
expect_verdict = "allow"

[[step]]
check = "cargo build --release"
[[step]]
expect_verdict = "allow"
