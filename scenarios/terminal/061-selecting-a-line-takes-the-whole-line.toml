name = "selecting by line takes the whole line, trailing blanks trimmed"
tags = ["selection"]

[[step]]
feed_lines = ["first", "the whole second line"]

[[step]]
select = ["3", "1"]
mode = "line"

[[step]]
expect_selection = "the whole second line"
